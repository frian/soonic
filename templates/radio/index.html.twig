<section id="radios" class="radios-view">
    {% for radio in radios %}
        <section class="radio clearfix">
            <div class="button-wrapper">
                <i class="icon-play radio-play"></i>
                <audio preload="none">
                    <source src="{{ radio.streamUrl }}" type="audio/mpeg"/>
                </audio>
            </div>
            <div class="radio-name">{{ radio.name }}</div>
            <div class="radio-url">{{ radio.homepageUrl }}</div>
        </section>
    {% else %}
    no radios found    
    {% endfor %}
    <!-- <ul> <li> <a href="{{ path('radio_new') }}">Create a new radio</a> </li> </ul> -->
    <!-- <audio id="player" controls preload="none"> <source id="mpeg-source" src="http://streaming.radionomy.com/Eurodance-90" type="audio/mpeg"/> </audio> -->
</section>
