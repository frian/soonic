{% extends 'base.html.twig' %}

{% block body %}

{% for song in songs %}

{{ song.title }} <br>

{% else %}
    <h1>no song</h1>
{% endfor %}

{#    <h1>{{ artist }}</h1>

    <table>
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ artist.name }}</td>
            </tr>
            <tr>
                <th>Coverartpath</th>
                <td>{{ artist.coverArtPath }}</td>
            </tr>
            <tr>
                <th>Albumcount</th>
                <td>{{ artist.albumCount }}</td>
            </tr>
            <tr>
                <th>Lastscanned</th>
                <td>{% if artist.lastScanned %}{{ artist.lastScanned|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Present</th>
                <td>{% if artist.present %}Yes{% else %}No{% endif %}</td>
            </tr>
            <tr>
                <th>Folderid</th>
                <td>{{ artist.folderId }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ artist.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('artist_index') }}">Back to the list</a>
        </li>
    </ul>#}
{% endblock %}
