{% extends 'base.html.twig' %}

{% block js %}
<script src="/js/radio.js"></script>
{% endblock %}

{% block body %}

<section id="radios" class="radios">


        {% for radio in radios %}

        <section class="radio clearfix">
            <div class="buttonWrapper">
                <i class="icon-play"></i>
                <audio preload="none">
                    <source src="{{ radio.streamUrl }}" type="audio/mpeg"/>
                </audio>
            </div>

            <div class="radioName">{{ radio.name }}</div>
            <div class="radioUrl">{{ radio.homepageUrl }}</div>
        </section>


            <!-- <tr>
                <td><a href="{{ path('radio_show', { 'id': radio.id }) }}">{{ radio.name }}</a></td>
                <td>{{ radio.streamUrl }}</td>
                <td>{{ radio.homepageUrl }}</td>
                <td>{% if radio.enabled %}Yes{% else %}No{% endif %}</td>
                <td>{% if radio.changed %}{{ radio.changed|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ radio.id }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('radio_show', { 'id': radio.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('radio_edit', { 'id': radio.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr> -->
        {% endfor %}

    <!-- <ul>
        <li>
            <a href="{{ path('radio_new') }}">Create a new radio</a>
        </li>
    </ul> -->

    <!-- <audio id="player" controls preload="none">
        <source id="mpegSource" src="http://streaming.radionomy.com/Eurodance-90" type="audio/mpeg"/>
    </audio> -->

</section>
{% endblock %}
